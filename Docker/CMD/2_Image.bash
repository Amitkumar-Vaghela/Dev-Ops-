docker images                 # List all images
docker pull ubuntu             # Download image from Docker Hub
docker pull nginx:latest       # Pull specific image version
docker rmi <image_id>          # Remove image
docker rmi -f <image_id>       # Force remove image
docker inspect <image_id>      # View detailed info about image
